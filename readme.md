# Home Work Process Mining

Make sure `pandoc` is installed on your system.
```
brew install pandoc
```

Generate pdf from jupyter notebook
```sh
jupyter nbconvert --to pdf src/main.ipynb --output hausarbeit_pm.pdf
```
